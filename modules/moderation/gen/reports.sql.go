// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: reports.sql

package gen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createReport = `-- name: CreateReport :one
INSERT INTO reports(reporter_id, reported_id, category, comment)
    VALUES ($1, $2, $3, $4)
RETURNING id, reporter_id, reported_id, category, comment, created_at
`

type CreateReportParams struct {
	ReporterID pgtype.UUID `db:"reporter_id" json:"reporter_id"`
	ReportedID pgtype.UUID `db:"reported_id" json:"reported_id"`
	Category   string      `db:"category" json:"category"`
	Comment    pgtype.Text `db:"comment" json:"comment"`
}

func (q *Queries) CreateReport(ctx context.Context, arg CreateReportParams) (Report, error) {
	row := q.db.QueryRow(ctx, createReport,
		arg.ReporterID,
		arg.ReportedID,
		arg.Category,
		arg.Comment,
	)
	var i Report
	err := row.Scan(
		&i.ID,
		&i.ReporterID,
		&i.ReportedID,
		&i.Category,
		&i.Comment,
		&i.CreatedAt,
	)
	return i, err
}

const listReportsByUser = `-- name: ListReportsByUser :many
SELECT id, reporter_id, reported_id, category, comment, created_at FROM reports WHERE reported_id = $1 ORDER BY created_at DESC
`

func (q *Queries) ListReportsByUser(ctx context.Context, userID pgtype.UUID) ([]Report, error) {
	rows, err := q.db.Query(ctx, listReportsByUser, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Report
	for rows.Next() {
		var i Report
		if err := rows.Scan(
			&i.ID,
			&i.ReporterID,
			&i.ReportedID,
			&i.Category,
			&i.Comment,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
